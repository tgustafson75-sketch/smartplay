# v1.0 Ship Readiness Audit — 2026-05-04

## TL;DR
- **Critical path status: RED**
- **Beta-this-week realistic: NO**
- **Top 3 blockers:**
  1. **Wrap 2B (Stripe) — NOT STARTED.** Paywall is a stub-only screen (`app/paywall.tsx:55-59` says "Stripe checkout will be available in the next update"). No Stripe SDK in `package.json`. No `STRIPE_*` env vars in Vercel. No webhook handler.
  2. **Empirical verification not run.** Both `docs/critical-paths.md` (last-verification dates: all four `_not verified_`) and `docs/audit-AX-empirical.md` (142 scenarios across 10 groups, every one `PENDING EMPIRICAL`) require real-device passes before external beta — explicitly per `critical-paths.md:228-230`.
  3. **Privacy policy URL missing.** Required by both App Store Connect and Play Console for store listing approval. Not present in `app.json`, `eas.json`, or `README.md`. Beta TestFlight needs it; Play internal track needs it.

---

## A. Workspace inventory

- **Tracked files:** 420 (`git ls-files`)
- **Routes (`app/**/*.tsx`):** 47 — 5 tabs, 8 onboarding (incl. `_layout`), 5 cage / cage-review, 5 swinglab, 5 arena, 2 recap, 1 round, 1 course (dynamic), settings + sub-route, 11 debug-named routes (see Phase E), plus auth/intro/index/etc.
- **Server-side handlers:** 14 in `api/*.ts` (Vercel) + 9 in `app/api/*+api.ts` (Expo Router server routes — duplicate surface; both reference the same env vars).
- **Components:** ~30 across `components/`, `components/caddie/`, `components/course/`, `components/kevin/`, `components/lieAnalysis/`, `components/recap/`, `components/smartfinder/`, `components/smartvision/`, `components/swinglab/`, `components/ui/`, `components/battery/`.
- **Services / lib / intents:** 105 files under `services/` (incl. `services/intents/` x10, `services/roles/` x3, `services/cage-analysis/` Python sidecar).
- **Stack versions:** Expo `~54.0.33`, React `19.1.0`, React Native `0.81.5`, TypeScript `~5.9.2`, Expo Router `~6.0.23`, `@sentry/react-native ~7.2.0`, `@anthropic-ai/sdk ^0.91.1`, `openai ^6.34.0`, `zustand ^5.0.12`.
- **Theme tokens:** **TWO theme systems coexist** — `theme/tokens.ts` (170 lines, full dark/light + high-contrast overlay via `composeTheme`, 13 semantic colors, spacing/typography/radii) and `constants/theme.ts` (54 lines, older `Colors` + `Fonts`, "always-dark" alias). Same token names, different shapes. Hardcoded hex outside theme: **1,911 hits** (top offender: `app/settings.tsx`, ~30 raw hex literals). Worth flagging.
- **Env configuration:**
  - **`.env.example`: DOES NOT EXIST.** No template; new clones have no roadmap to required env.
  - **Vercel:** 8 vars, all scoped **Preview + Production only** (Development scope empty): `EXPO_PUBLIC_MAPBOX_TOKEN`, `MAPBOX_ACCESS_TOKEN`, `WEATHER_API_KEY`, `GOLFCOURSE_API_KEY`, `ANTHROPIC_API_KEY`, `OPENAI_API_KEY`, `ELEVENLABS_API_KEY`, `YOUTUBE_API_KEY`. Five flagged "Sensitive" → values can't be re-pulled.
  - **`eas.json`** ships `EXPO_PUBLIC_API_URL=https://smartplay-beta.vercel.app`, `EXPO_PUBLIC_GOOGLE_MAPS_KEY=AIzaSyCh6a4PaRpohas6kmh1KjrmdYDQkIZuth4` (committed to git), `SENTRY_DISABLE_AUTO_UPLOAD=true` — identical across all three build profiles.
  - **EAS CLI:** not installed locally — `eas env:list` could not be executed. Vercel CLI was used for env enumeration instead.

---

## B. Shipped-claims verification

| Claim | Status | Evidence |
| --- | --- | --- |
| Phase V.6 — three stacking upload/analysis bugs | **CONFIRMED** | `2e5f4d0` "Phase V.6 Components 1-3: upload analysis reliability — single-swing classifier branch + duration probe fallback + stage diagnostics + tentative-read caveat"; `f2a8ce1` "Phase V.6 Components 4-6: latency naturalization"; `fe06bec` "Phase V.6 diagnostic-only: [V6-DIAG] tagged trace logs". No reverts in surrounding 50 commits. |
| Phase V.7 — re-analyze button | **CONFIRMED** | `app/lie-analysis.tsx:248` ("Re-analyze same photo") and `app/swinglab/swing/[swing_id].tsx:159,259,279,283,357,363` (`onReanalyze` handler + `reanalyzeBtn` styles, wired via `onPress`). Both routes reachable. |
| `SPEAK_TIMEOUT_MS` hard fix | **PARTIAL** | `services/voiceService.ts:155` — `const SPEAK_TIMEOUT_MS = 30_000;`. Used at line 171 only as a **fallback** when `durationMs` is unknown or `<= 0`. It is a **soft hint, not a hard cap** on actual TTS duration. If the claim was "hard cap on long responses," the constant exists but only kicks in absent a real duration probe. |
| Sentry runtime (Wrap 2A), auto-upload OFF | **PARTIAL** | Code wired: `import * as Sentry from '@sentry/react-native'` (`app/_layout.tsx:6`), gated init at `:59` (`if (process.env.EXPO_PUBLIC_SENTRY_DSN) { Sentry.init({...}) }`), `<Sentry.ErrorBoundary>` wrap at `:428-438`, `@sentry/react-native ~7.2.0` in deps. TODO marker at `:56`: `"TODO (Wednesday MacBook setup): add EXPO_PUBLIC_SENTRY_DSN + Sentry org/project to eas.json, then remove SENTRY_DISABLE_AUTO_UPLOAD=true from eas.json build profiles."` `SENTRY_DISABLE_AUTO_UPLOAD=true` confirmed in all three `eas.json` profiles. **Runtime gap:** `EXPO_PUBLIC_SENTRY_DSN` is not set anywhere (Vercel, eas.json, .env). Sentry init never fires today; capture is dead. `services/analytics.ts` comment: "Today the only sink is Sentry breadcrumbs." |
| Theme tokens (Phase 1) | **PARTIAL** | `theme/tokens.ts` ships dark + light + high-contrast variants properly. **Conflict:** `constants/theme.ts` ships an older `Colors` object that aliases light → dark (it's "always dark"). Both shipped. **1,911 hardcoded hex literals** outside the theme directory; `app/settings.tsx` alone has ~30 raw hex codes. Theme migration is partial. |
| Onboarding | **CONFIRMED** | `app/onboarding/_layout.tsx` + 7 screens: `welcome`, `name`, `mode`, `home-course`, `meet-kevin`, `about-game`, `ready`. Matches `critical-paths.md` Path 1. |
| Paywall UI | **CONFIRMED stub** | `app/paywall.tsx` exists; `services/paywallGuard.ts` exists. Paywall renders, but `paywall.tsx:55-59` shows: `// Stripe integration wired in Wrap Layer 2B` and the user-visible message: `"Stripe checkout will be available in the next update. Your trial continues."` Restore-purchase button is a no-op alert (`:66`). NOT WIRED to Stripe. |
| Analytics | **STUB** | `services/analytics.ts` is an in-house batcher. Only sink: Sentry breadcrumbs (file comment line 6). No PostHog, Amplitude, Segment, Mixpanel, or Firebase Analytics SDK in `package.json`. Sentry breadcrumbs require Sentry init, which doesn't fire (see Sentry row). Analytics is effectively off. |
| Landscape | **CONFIRMED unlocked** | `app.json:6` — `"orientation": "default"` (allows portrait + landscape). `expo-screen-orientation` plugin loaded. |
| Canonical Kevin layout (April 26) | **CONFIRMED** | `CLAUDE.md` locks `19165fb` (2026-04-26 12:43 PDT — "Fix Caddie screen: avatar framing, greeting bubble position, safe area"). Subsequent restore commits in window: `b69b6f3` "Phase AU - Kevin canonical restore"; `084da48` "Hard reset layout files to 19165fb canonical state". Component files: `components/CaddieAvatar.tsx` (32,315 bytes), `components/LivingKevin.tsx` (4,512 bytes), `components/kevin/KevinAvatar.tsx` (4,912 bytes). Container locked in `app/(tabs)/caddie.tsx` per CLAUDE.md L4 trustLevel block. |

---

## C. Critical path

### C1. Stripe (Wrap 2B): **NOT STARTED**

- **Evidence:**
  - `grep -rni stripe app/ components/ services/ api/`: only 2 hits in code, both stub strings in `app/paywall.tsx` (`:55` comment, `:59` alert text). Other "stripe" matches in `api/parse-shot.ts:14,27` and `app/api/parse-shot+api.ts:13` are unrelated golf slang ("striped").
  - `package.json` deps: no `stripe`, no `@stripe/stripe-react-native`, no `react-native-purchases` (RevenueCat). Confirmed via `jq '.dependencies | with_entries(select(.key | test("stripe|revenuecat|in.app"; "i")))'` → `{}`.
  - Vercel env vars: no `STRIPE_*` or `EXPO_PUBLIC_STRIPE_*` (confirmed via `vercel env ls`).
  - No webhook handler in `api/`, `app/api/`. No `supabase/` directory exists.
- **Estimated remaining work: L (~12-20 hours)**
  - Stripe SDK + native config (iOS/Android)
  - Wire `app/paywall.tsx` to real PaymentSheet / subscription flow
  - Vercel API route for webhook (`/api/stripe-webhook`) with signature verification
  - Stripe products/prices defined in dashboard
  - App Store Connect IAP if iOS subscription path chosen instead (StoreKit 2 ≠ Stripe — clarify which platform)
  - Receipt validation server-side
  - End-to-end test pass

### C2. TestFlight / Play Store (Wrap 3): **PARTIALLY CONFIGURED**

- **Build profiles:** `eas.json` defines `development`, `preview`, `production`. `submit.production: {}` is empty — no Apple/Google submission credentials configured.
- **Bundle / package IDs:** `com.smartplaycaddie.app` set for both iOS and Android. ✓
- **Version / build:** `version: "1.0.0"` set. No `buildNumber` or `versionCode` in `app.json` — EAS will manage these (`appVersionSource: "local"`). ✓
- **Icons / splash:** present (`assets/images/icon.png`, `android-icon-foreground/background/monochrome.png`, `splash-icon.png`). ✓
- **Privacy policy URL: MISSING.** Not in `app.json`, `eas.json`, or `README.md`. Required for store listing.
- **Native folders:** `/ios` and `/android` are gitignored (Expo prebuild generates them). No leftover stub.
- **Manual checks Tim must do:**
  1. App registered in **App Store Connect** with bundle ID `com.smartplaycaddie.app`?
  2. App registered in **Google Play Console** with package `com.smartplaycaddie.app`?
  3. Support email (e.g., `support@smartplaycaddie.com`) configured in both consoles?
  4. **Privacy policy URL exists at a publicly accessible URL** and is referenced in both store listings? (This is a hard requirement for both stores.)
  5. EAS Submit credentials configured (`eas credentials` for iOS App Store Connect API key + Android service account JSON)?

### C3. Four verification gates: **DOCUMENTED, ZERO RUN**

Found in `docs/critical-paths.md` and elaborated in `docs/audit-AX-empirical.md`. Reproduced verbatim from `critical-paths.md`:

> **Path 1 — ONBOARD.** Cold install → onboarding screens → Caddie home with profile populated.
>
> **Path 2 — ROUND.** Open app → find course → start round → play through holes → log shots → end round → see recap on scorecard.
>
> **Path 3 — CAGE.** Open SwingLab → Cage Mode setup → record session → analysis → drill recommendation → open drill.
>
> **Path 4 — VOICE.** Earbud tap (or on-screen Kevin badge) → Kevin engages → query → response → continuation or close.
>
> **Beta-readiness verdict:** External beta requires: all four critical paths verified end-to-end on a real device within the last 7 days, on a real round (not just simulated). Until that's true: internal personal beta only. No external testers.
>
> **Last verification dates** (update after each MIN VERIFY pass):
> - Path 1 ONBOARD: _not verified_
> - Path 2 ROUND: _not verified_
> - Path 3 CAGE: _not verified_
> - Path 4 VOICE: _not verified_

`docs/audit-AX-empirical.md` defines 142 numbered scenarios (AX-1 through AX-142) across 10 groups (App Startup, Navigation/Layout, Kevin Position, Theme/Contrast, Voice Flow, GPS/Round-Active, Cage/Upload, Play/Arena, Scorecard/Recap, Cross-Pillar). **Every scenario currently STATUS: PENDING EMPIRICAL.** No PASSED verdicts recorded.

By the project's own gating rule (`CLAUDE.md` "Critical Path Verification Gates" + `critical-paths.md:228-247`): external beta is blocked until all four paths verify on a real device within 7 days. **None have verified ever.**

---

## D. Health checks

- **tsc (`npx tsc --noEmit`)**: ✅ **0 errors**, exit 0.
- **lint (`npm run lint` → `expo lint`)**: ❌ **1 error, 8 warnings**. Exit 1.
  - Error: `app/diagnostic-card.tsx:157` — `react/no-unescaped-entities` (an apostrophe needs escaping). Single-character fix.
  - Warnings (all in `app/(tabs)/caddie.tsx` except one): unused imports `Image`, `SmartFinderCard`, `KevinAvatar`; unused vars `saverActive`, `kevinAvatarState`, `handleChangeModePress`; one `react-hooks/exhaustive-deps` (`useMemo` unnecessary `markTick`); `app/smartvision.tsx:100` unused `projectToPixels`.
- **tests (`npm test`)**: ❌ **No `test` script defined in `package.json`**. There is no test framework installed. The audit prompt's `--passWithNoTests` flag couldn't be applied. **0/0 tests** — testing infrastructure absent.
- **expo-doctor**: ✅ **17/17 checks passed. No issues detected.** (Run via `npx expo-doctor`, version 1.18.21.)
- **EAS build dry-run**: not run. `eas` CLI not installed locally. `eas.json` parses cleanly (verified by `cat | jq`).

---

## E. Polish debt

- **TODO/FIXME/HACK/XXX count:** **2** (very clean for a 420-file project).
  - `app/_layout.tsx:56` — `// TODO (Wednesday MacBook setup): add EXPO_PUBLIC_SENTRY_DSN + Sentry org/project to eas.json, then remove SENTRY_DISABLE_AUTO_UPLOAD=true from eas.json build profiles.`
  - `services/intents/queryStatusHandler.ts:563` — `const driverYards = 230; // TODO: read from accumulated club distances when wired`
- **Top concerning TODOs:** Both above are real but small-scope. The Sentry one is the same gap surfaced in B/Sentry. The handicap/club-distance hardcode could affect Kevin's distance-aware responses but is fallback-only.
- **`console.log` count:** **196** in `app/`, `components/`, `services/`, `hooks/`, `contexts/` (excluding `__tests__`). Significant for a v1.0 ship. Most are tagged with prefixes like `[smartfinder]`, `[V6-DIAG]`, `[lie-analysis]` — they doubled as dev diagnostics. With Sentry breadcrumbs effectively off, removing them now would lose the only runtime trace. Recommend gating behind `__DEV__` or migrating to `Sentry.addBreadcrumb()` in a 1.0.x cleanup.
- **Placeholder strings (user-visible):**
  - `app/auth.tsx:7` — `"Auth — coming soon"` shown as the entire screen content. **User-visible placeholder; concerning if `/auth` is reachable in 1.0.**
  - `app/settings.tsx:401` — `"Earbud Tap-to-Talk · Coming soon"` (per `critical-paths.md:189` this is a known stub — native Bluetooth media-key listener is intentionally deferred).
  - `app/settings.tsx:496` — `"Samsung Health SDK integration coming soon. Currently runs in simulation mode for testing."`
  - `app/arena/scramble.tsx:11` — `"Coming soon — multiplayer scramble with Kevin tracking best balls"` (Arena content; see Phase F).
- **Debug-only routes shipped in `app/`** (will be reachable via deep link unless gated): 11 routes — `api-debug.tsx`, `battery-debug.tsx`, `cage-debug.tsx`, `ghost-debug.tsx`, `kevin-learning.tsx`, `landmark-curate.tsx`, `patterns-debug.tsx`, `plan-debug.tsx`, `smartfinder-debug.tsx`, `subscription-debug.tsx`, `voice-debug.tsx`. Recommend hiding these from production navigation or guarding behind `__DEV__` for v1.0.

---

## F. Future-feature contamination check

- **HeyGen / LiveAvatar:** **absent** ✓
- **Serena:** **PRESENT** at `app/intro.tsx:78,118,151,155,166`, `app/settings.tsx:309,542`, `components/CaddieAvatar.tsx:44-46`. Wired as the female-voice variant of the caddie character (Kevin / Serena selector). If "Serena" was meant to be deferred per the audit's expectation, this is shipping today as a voice/persona option. **FLAG — confirm with Tim whether the Serena voice option was intended for v1.0 or carried over from a deferred persona scope.**
- **Arena:** **PRESENT** — full directory `app/arena/` with `_layout.tsx`, `index.tsx`, `ctp.tsx`, `scramble.tsx`, `sim-round.tsx`, `skills.tsx`. Registered in `app/_layout.tsx:260` (`<Stack.Screen name="arena" />`). Referenced in `app/(tabs)/swinglab.tsx:25,34,126,128` (drill environment) and `app/(tabs)/caddie.tsx:951-991` (active round Arena context). The audit prompt expected Arena to be absent. **FLAG.** (Note: `audit-AX-empirical.md` Group 8 explicitly verifies Arena scenarios AX-119–AX-125, and `critical-paths.md` does not list Arena as a critical path — it's not gating beta, but it IS a 1.0 surface today.)
- **Ray-Ban / smart-glasses:** **absent** ✓
- **`player_id`:** **PRESENT in data models only** — `services/cageStorage.ts:50,166`, `store/roundStore.ts:83,343,519`. Line 83 explicitly: `player_id?: string; // reserved for Phase 1.1 multi-player`. **Correct placement** per the audit's standing rule (data-model future-proofing, not UI-wired).
- **golfcourseapi:** **PRESENT** — `services/golfCourseApi.ts` is the live client; called from `app/api-debug.tsx:14`, `app/course/[course_id].tsx:11,36`, and via `services/golfCourseApi.ts` proxied to `/api/course-proxy` (`api/course-proxy.ts:27`, `api/kevin.ts:83`, `api/course-geometry.ts:162`). Recent commit `26c175e Play tab GolfCourseAPI search: fix shape, surface key-missing error` confirms the Play tab is wired. **FLAG — the audit says these should be absent post-brain-rewrite.** Either the brain rewrite is complete and these are leftover, or the rewrite is incomplete. Tim to confirm intent.
- **Google Places:** **absent** ✓
- **YouTube:** **PRESENT** at `app/(tabs)/swinglab.tsx:447,451,454,468,471` — opens `youtube.com/results?search_query=...` via `Linking.openURL` from drill cards. `YOUTUBE_API_KEY` is provisioned in Vercel preview/production. Used as instructional-video discovery for SwingLab drills. **FLAG.**

---

## G. Recommendations (audit author's call, not Tim's standing decision)

- **Hole-image pipeline: recommend 1.0.1.**
- **Reasoning:** v1.0 has three hard ship blockers that have nothing to do with hole imagery — Stripe (Wrap 2B) is not started, the privacy policy URL is missing, and zero of the four critical paths have empirical verification on file. Adding hole-image scope to v1.0 would push the beta further while not unblocking what's actually blocking. The right move is to ship v1.0 with the existing Mapbox tile + bundled Palms hero images path (already working per Phase AV/AW) and queue the hole-image enhancement for 1.0.1, where it lands on a stable beta and gets its own focused verification cycle. If 1.0 slips past mid-May regardless, revisit — but the slip would come from Wrap 2B/3, not from the imagery decision.

---

## H. What this audit did NOT check (so Tim isn't surprised)

- Real-device behavior on Galaxy Z Fold (closed + open) — explicitly out of scope for code-level audit per `audit-AX-empirical.md` operating principles.
- Stripe end-to-end purchase flow.
- App Store Connect / Google Play Console state (registration, internal testing track readiness, support contact).
- Live API calls to Anthropic / OpenAI / ElevenLabs (`process.env.*_API_KEY` present in Vercel but not exercised here).
- Audio playback latency / TTFA on real device.
- Bluetooth earbud media-key callbacks (native Kotlin module is a known stub per Phase AC).
- GPS hardware behavior outdoors with real fixes.
- EAS build/submit dry-run (CLI not installed; `eas credentials` state unknown).
- Privacy policy content compliance (not just URL presence — needs human review against App Store + Play policies).
- Any Vercel project-side checks (CSP headers, deploy logs, function timeouts) beyond env-var enumeration.
